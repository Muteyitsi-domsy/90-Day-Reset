# Sentry Configuration
# Get your DSN from https://sentry.io/settings/projects/
# This is safe to include in client-side code
VITE_SENTRY_DSN=your_sentry_dsn_here

# Firebase Configuration
# Get these values from Firebase Console > Project Settings > General
# These are safe to include in client-side code
VITE_FIREBASE_API_KEY=your_firebase_api_key
VITE_FIREBASE_AUTH_DOMAIN=your_project_id.firebaseapp.com
VITE_FIREBASE_PROJECT_ID=your_project_id
VITE_FIREBASE_STORAGE_BUCKET=your_project_id.appspot.com
VITE_FIREBASE_MESSAGING_SENDER_ID=your_sender_id
VITE_FIREBASE_APP_ID=your_app_id

# ============================================
# AI CONFIGURATION
# ============================================

# Vertex AI (Recommended for Production)
# Set to 'true' to use Vertex AI (unlimited quota, pay-as-you-go)
# Set to 'false' to use Gemini API (free tier with quota limits)
# See VERTEX_AI_SETUP.md for complete setup instructions
VITE_USE_VERTEX_AI=false

# Vertex AI API endpoint (default points to Vercel serverless function)
VITE_VERTEX_API_URL=/api/vertex-ai

# Gemini API (Development/Fallback)
# Get your free API key at: https://aistudio.google.com/app/apikey
# Note: Free tier has quota limits (15 requests/minute)
VITE_GEMINI_API_KEY=your_gemini_api_key_here

# ============================================
# VERCEL SERVERLESS FUNCTION VARIABLES
# ============================================
# Add these to your Vercel project settings ONLY
# Dashboard > Settings > Environment Variables
# DO NOT add these to .env.local (they are server-side only)

# VERTEX_PROJECT_ID=your-gcp-project-id
# VERTEX_LOCATION=us-central1
# GOOGLE_APPLICATION_CREDENTIALS_JSON={"type":"service_account","project_id":"..."}

# Rate Limiting & Cost Controls (configured in api/vertex-ai.ts):
# - Daily limit: 50 requests per user per day
# - Monthly cap: $100 total spending
# Modify the constants in api/vertex-ai.ts to adjust these limits

# ============================================
# SUBSCRIPTION / REVENUECAT CONFIGURATION
# ============================================
# Get your API keys from RevenueCat Dashboard > API Keys
# See SUBSCRIPTION_SETUP.md for complete setup instructions

# RevenueCat Public SDK Key for Android
VITE_REVENUECAT_ANDROID_KEY=your_revenuecat_android_key_here

# RevenueCat Public SDK Key for iOS (if applicable)
VITE_REVENUECAT_IOS_KEY=your_revenuecat_ios_key_here

# ============================================
# CAPACITOR / MOBILE APP CONFIGURATION
# ============================================
# App ID must match what's registered in Play Store
# Current: app.renew90.journal
